# ── Env / secrets ────────────────────────────────────────────────────────────
.env
.env.*
.env.local
.envrc
!.env.example
!**/.env.example
!.env.vps.example
.env.relayer

# ── OS / IDE ──────────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
.roommodes
.roo/
.claude/
.aider*

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
*.log

# ── Next.js ───────────────────────────────────────────────────────────────────
.next/
frontend/.next/
frontend/cache/

# ── Python (script/check_storage_structs.py) ──────────────────────────────────
__pycache__/
*.pyc

# ── Foundry build artifacts (regenerated by `forge build`) ───────────────────
out/
artifacts/
cache/
# Deployment broadcast logs — public TX data, excluded to keep repo clean
broadcast/

# ── Docker / local services ───────────────────────────────────────────────────
qdrant_storage/

# ── Subgraph build artifacts ──────────────────────────────────────────────────
subgraph/build/
subgraph/generated/

# ── Relayer runtime data ──────────────────────────────────────────────────────
relayer/storage/

# ── Personal notes / drafts (not documentation) ──────────────────────────────
docs/ord.txt
docs/nextplan.txt
docs/nfix.txt
docs/fail.txt
docs/ArchCheckFacets.txt
docs/depversel.txt
docs/GlobalAudit.txt
docs/CHAT_PROMPT.md
docs/CLAUDE_CODE_PROMPT.md
docs/thoughts/

deploy-local.sh

# ── Superpowers brainstorming companion (local mockups, not project artifacts) ─
.superpowers/

# ── Superpowers isolated worktrees ────────────────────────────────────────────
.worktrees/
# Real user push endpoints + auth keys — never commit
relayer/push_subscriptions.json
relayer/storage/
