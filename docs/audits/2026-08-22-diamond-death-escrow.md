# Hexseal — деньги живой сделки, когда даймонд перестал отвечать

**Дата:** 2026-08-22
**Замок:** `test/DiamondDeathEscrow.t.sol` (38 тестов)
**Предмет:** `src/Agreement.sol` — каждая сделка это клон EIP-1167, деньги лежат
в клоне, даймонд используется как реестр, репутация и арбитраж.

Вопрос был поставлен так: **для каждого пути, которым USDC выходит из
`Agreement`, выходят они или нет, когда даймонд не отвечает.** Ответ ниже —
замером, а не чтением кода.

---

## Ответ одной фразой

**Обещание сохранности верно для всех сделок, кроме спорных.** Деньги
неспорной сделки выходят из эскроу при мёртвом даймонде — все пути наружу,
кроме одного, защищены. **Спорная сделка запирается намертво: единственный
выход из статуса `DISPUTED` стоит за голой проверкой на диамонде
(`src/Agreement.sol:875`), и когда диамонд молчит, котёл не достаётся никому
и никогда.** Второй двери у спора нет — это тоже замерено, а не рассуждение.

Плюс два уточнения, каждое со своей ценой:

* при фасете, который **жжёт газ** вместо реверта, `try/catch` не спасает и
  неспорные пути тоже встают (нужно ~71× газа сверх обычного);
* при мёртвом даймонде сторона **всё ещё может поднять спор** и войти в
  запертое состояние — ничто её не предупреждает.

---

## Что считается «даймонд не отвечает» — три разных вида

Они не взаимозаменяемы, и это оказалось важно.

| вид | как воспроизведён | что происходит |
|---|---|---|
| **A. селектор снят** | `diamondCut` с `Remove` на все 7 селекторов, которые зовёт `Agreement` | фоллбэк прокси падает на `require(facet != address(0))` |
| **B. фасет ревертит** | `diamondCut` с `Replace` на контракт, который ревертит на всём | селектор смонтирован, ответа нет |
| **C. кода нет вовсе** | `vm.etch(diamond, "")` | вызов не доходит до кода — и это ломается ИНАЧЕ, см. ниже |

Семь селекторов, которые `Agreement` зовёт у диамонда:
`updateStatus`, `autoAwardXP`, `notifyExecutorFault`, `notifyArbiterTimeout`,
`hasSubmittedVerdict`, `creditDisputeFee`, `clearDisputeClaim`.

---

## Таблица: путь → вид отказа → вышли ли деньги

| путь (кто зовёт) | статус сделки | A: селектор снят | B: фасет ревертит | C: кода нет | если нет — что держит |
|---|---|---|---|---|---|
| `release()` (клиент) | ACTIVE + markDone | **да** | **да** | **нет** | `Agreement.sol:1262` (см. §3) |
| `triggerAutoApprove()` (любой) | markDone + окно вышло | **да** | **да** | **нет** | `Agreement.sol:1262` |
| `triggerActivationTimeout()` (сторона) | FUNDED, окно вышло | **да** | **да** | **нет** | `Agreement.sol:1262` |
| `triggerDeadlineTimeout()` (сторона) | ACTIVE, дедлайн вышел | **да** | **да** | **нет** | `Agreement.sol:1262` |
| `rejectExtra()` (исполнитель) | есть ожидающий extra | **да** | **да** | **да** | — диамонд не зовётся вообще |
| `triggerArbiterTimeout()`, спор никем не взят | DISPUTED, окно вышло | **НЕТ** | **НЕТ** | **НЕТ** | **`Agreement.sol:875`** |
| `triggerArbiterTimeout()`, спор взят арбитром | DISPUTED, окно вышло | **НЕТ** | **НЕТ** | **НЕТ** | **`Agreement.sol:875`** |
| `resolveDispute()` (арбитр) | DISPUTED | **НЕТ** | **НЕТ** | **НЕТ** | достижим только через диамонд (`claimDispute` ставит арбитром сам диамонд) |
| `syncRegistry()` — денег не двигает | любой | нет | нет | нет | `Agreement.sol:1281` (голая, при том что 1262 та же функция под `try`) |

Отдельной строкой, потому что об этом спрашивали прямо:

> **Автоапрув по таймауту от диамонда НЕ зависит** — при видах A и B деньги
> исполнителю выходят. Худшая находка не подтвердилась. Но см. §4 про газ.

---

## 1. Главная находка: `Agreement.sol:875` запирает спорную сделку

```solidity
// src/Agreement.sol:875, внутри triggerArbiterTimeout()
if (IArbiterRegistryFacet(diamond).hasSubmittedVerdict(address(this))) revert VerdictInFlight();
```

Голый вызов, без `try/catch`, и стоит он **до** `_settlePending()` — то есть
до того, как хоть цент двинулся. Диамонд молчит → реверт → выхода нет.

Проверка сама по себе осмысленная (не давать форсировать рефанд, пока вердикт
летит через `FINALIZE_DELAY`/апелляцию). Дефект не в её смысле, а в том, что
**недоступность ответа приравнена к «вердикт есть»**, хотя по смыслу должна
приравниваться к «вердикта нет».

Сравнение внутри того же файла показывает, что это недосмотр, а не решение:
все остальные обращения к диамонду обёрнуты, и у `resolveDispute` даже стоит
комментарий, объясняющий ровно эту опасность:

> *«Провал зачисления ТЕРПИМ: иначе сломанный или снятый на диамонде селектор
> сделал бы спор незакрываемым, а деньги встали бы в эскроу навсегда.»*
> — `src/Agreement.sol`, около строки 786

Автор эту угрозу знал и защитился от неё на строке 795. На строке 875 —
не защитился.

### Второй двери у спора нет — замерено

`test/DiamondDeathEscrow.t.sol::testDisputedDealHasNoOtherExit_*` берёт спорную
сделку с мёртвым диамондом и стучится во все двери подряд:

| дверь | результат | почему |
|---|---|---|
| `release()` | реверт | `AlreadyDisputed` |
| `triggerAutoApprove()` | реверт | `AlreadyDisputed` |
| `triggerDeadlineTimeout()` | реверт | `AlreadyDisputed` |
| `triggerActivationTimeout()` | реверт | требует `activatedAt == 0` |
| `resolveDispute(bool)` от арбитра-человека | реверт | `NotArbiter` — арбитр это сам диамонд |
| `resolveDispute(bool)` от кого угодно | реверт | то же |
| `triggerArbiterTimeout()` | реверт | **строка 875** |

Баланс клона после всех семи попыток — не изменился. Функции спасения в
`Agreement` не существует; `owner` диамонда достать эти деньги не может тоже,
потому что деньги не на диамонде.

### И в ловушку можно войти уже во время аварии

`raiseDispute()` трогает диамонд только через `_updateRegistry`, а тот
обёрнут. Значит при видах A и B **поднять спор по-прежнему получается** —
и сделка проваливается в состояние без выхода. Ни экран, ни контракт об этом
не предупреждают.

Замер: `testDisputeCanStillBeRaisedWhileDiamondIsDead_SelectorsRemoved`,
`..._FacetReverts`.

---

## 2. Что защищено и работает

Пять путей выходят при видах A и B без единой оговорки:
`release`, `triggerAutoApprove`, `triggerActivationTimeout`,
`triggerDeadlineTimeout`, `rejectExtra`.

Защита не декоративная — это проверено отдельно, а не выведено из факта, что
деньги ушли (деньги ушли бы и в том случае, если бы вызова к диамонду не было
вовсе). `testReleaseUnderDeadDiamondLeavesRegistryStaleAndSaysSo` показывает
всю тройку разом:

* деньги ушли исполнителю (`DEAL` на балансе),
* запись в реестре **осталась старой** (`ACTIVE`, а не `COMPLETED`) — значит
  вызов действительно провалился,
* событие `RegistrySyncFailed` сработало — значит выполнилась именно ветка
  `catch`.

Цена, которую платит протокол за уцелевшие деньги: реестр расходится с
правдой, XP не начисляется (`testXpIsSilentlySkippedWhenDiamondIsDead`),
`clearDisputeClaim` не отрабатывает. Это и задумано — «деньги важнее реестра».

---

## 3. Ловушка вида C: `try/catch` НЕ ловит проверку размера кода

Отдельная находка, и она меняет представление о том, насколько крепок
`try/catch`.

Замерено на отдельном стенде (`testTryCatchDoesNotCatchExtcodesizeGuard`),
намеренно не завязанном на код `Agreement`:

| форма вызова | адрес без кода |
|---|---|
| `try IFacet(a).f() {} catch {}`, **без** возвращаемого значения | **внешний фрейм ревертит, `catch` НЕ выполняется** |
| голый `IFacet(a).f()` **с** возвращаемым значением | реверт (декодер: `returndatasize` = 0) |
| `a.staticcall(...)` | **`ok == true`**, данных 0 байт |

Причина: для вызова без возвращаемых данных solc вставляет проверку
`extcodesize` **в кадр вызывающего, до опкода `CALL`**. `try/catch` смотрит
на флаг успеха от `CALL` — а до него дело не доходит. Реверт летит наружу
мимо `catch`.

Следствие для нас: при виде C ломается **каждый** путь через `_complete()`,
включая автоапрув. Именно поэтому в таблице весь столбец C красный.

### Насколько это достижимо на живом контракте — честно: сегодня нет

* у `DiamondProxy` нет `selfdestruct`, а после Cancun он всё равно работает
  только в транзакции создания — прокси свой код потерять не может;
* адрес диамонда пишется в `FactoryStorage` один раз в `initFactory`, сеттера
  нет;
* и даже при подмене `deployAgreement` упал бы раньше создания клона, на
  `IRegistry(fs.diamond).hasActivePair(...)` — вызов с возвращаемым значением
  ревертит на адресе без кода.

Так что вид C — это **свойство формы, а не живая дыра**. Записан по двум
причинам. Первая: он объясняет, почему «обернуть 875 в `try/catch`» —
неполное лекарство. Вторая: `AgreementDeployer` проверяет `code.length > 0`
у реализации (и в комментарии подробно объясняет, зачем), а у **диамонда**
такой проверки нет ни в `Agreement.initialize`, ни в `initFactory` — стоит
кому-нибудь добавить сеттер адреса диамонда, и вид C становится живым.

### Третья форма, из той же таблицы

`Agreement.setArbiter` (строка 495) читает реестр низкоуровневым
`staticcall` и пишет `if (!ok || !abi.decode(data, (bool)))`. На адресе без
кода `ok` приходит **`true`** с пустыми данными: половина условия `!ok`
мертва, и реверт приходит не из задуманной ветки `ArbiterNotRegistered`, а из
`abi.decode`. Денег не касается, но это ровно та ловушка, о которой
предупреждали.

---

## 4. Ловушка с газом: `try/catch` не спасает от фасета, который жжёт газ

`try/catch` превращает реверт в пойманную неудачу, но **не возвращает газ**,
уже сожжённый вызываемым. По EIP-150 вызываемому уходит 63/64 остатка; у нас
между `Agreement` и фасетом стоит ещё один кадр (прокси → `delegatecall`),
поэтому одно логическое обращение к диамонду стоит примерно **1/32** остатка,
если фасет съедает всё.

`Agreement._complete()` делает **два** таких обращения подряд **до** перевода:
`_updateRegistry` (строка 1262), затем `autoAwardXP` (строка 1253).

Замер (фасет — бесконечный цикл `SSTORE` на селекторах `updateStatus` и
`autoAwardXP`, бюджет 30 000 000):

```
газ до обращения 1:   29 999 784
газ после обращения 1:   929 412
газ после обращения 2:    28 067
```

| что | газа нужно |
|---|---|
| `triggerAutoApprove()` при здоровом даймонде | ~419 481 |
| `triggerAutoApprove()` при жгущем фасете | ~29 791 258 |

**Разница ~71×.** При любом реалистичном лимите транзакции деньги не выходят;
кошелёк такой лимит не поставит сам, а если бы и поставил — это прямые деньги
за газ.

Пути рефанда страдают меньше: у них до перевода стоит **одно** обращение
(`notifyExecutorFault` идёт уже после денег), потеря 1/32 один раз, и при
1 000 000 газа они проходят. То есть ущерб масштабируется числом обращений к
диамонду, стоящих **перед** выплатой.

Замеры: `testGasBurningFacetBlocksAutoApproveAtARealisticGasBudget`,
`testAutoApproveFitsInARealisticGasBudgetWhenDiamondIsHealthy`,
`testGasBurningFacetIsBeatenOnlyByAnAbsurdGasBudget`,
`testGasBurningFacetDoesNotBlockActivationTimeout`.

Бесконечный цикл — не экзотика: любой неограниченный обход растущих данных
приходит к этому сам, по мере роста.

---

## 5. Дисциплина замера: чем доказано, что тест не смотрится в зеркало

Числа красных мало — надо называть, **кто** покраснел и **тот ли это замок**,
ради которого мутация ставилась. Четыре мутации, все прогнаны на полном
наборе (994 теста до добавления последних трёх; база — зелёная).

### М1. Снять `try/catch` с защищённого вызова (`_updateRegistry`, строка 1262)

Требование брифа дословно: тест обязан краснеть, если убрать защиту.

**13 красных, все 13 — в новом наборе:**

```
testRelease_SelectorsRemoved_MoneyOut          "release must survive a removed selector"
testRelease_FacetReverts_MoneyOut              "release must survive a reverting facet"
testAutoApprove_SelectorsRemoved_MoneyOut      "auto-approve must survive a removed selector"
testAutoApprove_FacetReverts_MoneyOut          "auto-approve must survive a reverting facet"
testActivationTimeout_SelectorsRemoved_MoneyOut"refund must survive a removed selector"
testActivationTimeout_FacetReverts_MoneyOut    "refund must survive a reverting facet"
testDeadlineTimeout_SelectorsRemoved_MoneyOut  "refund must survive a removed selector"
testDeadlineTimeout_FacetReverts_MoneyOut      "refund must survive a reverting facet"
testReleaseUnderDeadDiamondLeavesRegistryStaleAndSaysSo
testXpIsSilentlySkippedWhenDiamondIsDead
testSyncRegistryIsBareAndDiesWithTheDiamond
testGasBurningFacetIsBeatenOnlyByAnAbsurdGasBudget
testGasBurningFacetDoesNotBlockActivationTimeout
```

Каждый покраснел **своим** сообщением, а не по чужой причине.

**И отдельно: из 959 существовавших до этого тестов покраснело НОЛЬ.**
Значит защиту на строке 1262 не сторожил никто — её можно было снять правкой
одной строки, и вся стойка осталась бы зелёной.

### М2. Обернуть строку 875 в `try/catch` (обратная мутация)

Нужна, чтобы доказать: вердикты «заперто» держатся именно за 875, а не за
случайного соседа.

**6 красных, все 6 — назначенные:**

```
testArbiterTimeoutUnclaimed_SelectorsRemoved_MoneyStUCK
testArbiterTimeoutUnclaimed_FacetReverts_MoneyStUCK
testArbiterTimeoutClaimed_SelectorsRemoved_MoneyStUCK
testArbiterTimeoutClaimed_FacetReverts_MoneyStUCK
testDisputedDealHasNoOtherExit_SelectorsRemoved  "the only real hatch, and it is blocked"
testDisputedDealHasNoOtherExit_FacetReverts      "the only real hatch, and it is blocked"
```

Варианты `_NoCode_` **остались зелёными** — и это правильный ответ, а не
пропуск: при виде C `try/catch` не помогает (см. §3), сделка остаётся
запертой строкой 1262. То есть мутация заодно показала, что одной обёртки
на 875 для полного лечения не хватит.

### М3. Обезвредить сам «убийца» (`_kill` не делает `diamondCut`)

Проверка на пустую мутацию: если бы убийство было холостым, все выводы
обнулились бы.

**19 красных**, из них **14 — от назначенного сторожа** с сообщением
`"kill did not actually silence the diamond"`. Сторож работает.

### М4. Не смонтировать `autoAwardXP`/`notifyExecutorFault` в стенде

Самая коварная форма пустой мутации: остальные наборы в репозитории эти
селекторы не монтируют, и, скопируй я их setUp дословно, «снятие селектора»
было бы **холостым** — снимать было бы нечего.

**23 красных**, и среди них назначенный:

```
testBaselineDiamondCallsAreLive  "autoAwardXP must land while alive: 0 <= 0"
```

Базовый замер честно ловит, что вызов не был живым.

### Откуда берётся ожидаемое

Ожидаемые суммы — рукописные литералы от `DEAL = 1 000 000 000` (`DEAL_HALF`
и т.д.), окна (`2 days`, `4 days`) переписаны в тесте константами, а не
импортированы из `Agreement`. Факт «деньги вышли» меряется дельтой баланса на
mock-USDC — контракте, которого даймонд не касается. Ожидаемое и фактическое
приходят из разных мест.

---

## 6. Что дальше

> ⚠️ **Дописано позже тем же днём: дыра починена.** Раздел «Починка» в конце
> файла — что именно сделано, чем замерено и что осталось открытым. Всё, что
> ниже в этом разделе, — постановка задачи на починку, а не описание текущего
> состояния кода.

Дыра **не чинилась** в этой работе — намеренно, по условию задачи. Заведены
пункты `docs/OPEN-ITEMS.md` **112** (строка 875), **113** (газ) и **114**
(проверка размера кода / строка 1281).

Что стоит помнить при проектировании починки:

1. обёртка на 875 обязана трактовать молчание как «вердикта нет» — иначе
   лечение просто меняет одну неверную трактовку на другую;
2. одной обёртки мало: вид C и газовая ловушка бьют через 1262 и лечатся
   иначе (порядок вызовов, лимит газа на обращения к диамонду, либо вынос
   обращений к диамонду **после** перевода денег);
3. `notifyExecutorFault` в таймаутах уже стоит **после** перевода — это
   готовый образец правильного порядка внутри того же файла.

---

## Приложение: как перепроверить

```bash
forge test --match-path test/DiamondDeathEscrow.t.sol -vv
```

38 тестов на момент замера; после починки — 43. Полный набор был 997 тестов,
стал 1002, зелёный.

---

# Починка

**Дата:** 2026-08-22 (тот же день). Правка — `src/Agreement.sol`, замок —
`test/DiamondDeathEscrow.t.sol` (было 38 тестов, стало 43). Полный набор:
997 → 1002, зелёный. В цепь ничего не писалось.

## Что стало правдой

| даймонд | было | стало |
|---|---|---|
| отвечает «вердикт подан» | реверт `VerdictInFlight` | **так же** |
| отвечает «не подан» | проходит | **так же** |
| селектор снят | реверт, деньги заперты | **проходит, деньги выходят** |
| фасет ревертит | реверт, деньги заперты | **проходит, деньги выходят** |
| кода на адресе нет | реверт, деньги заперты | **проходит, деньги выходят** |
| газа не хватает на честную передачу капа | — | **реверт всей транзакции**, `NotEnoughGasForVerdictCheck` |

## Форма: две правки, одна мысль

### 1. `_verdictInFlight()` — вместо голого вызова на 875

Низкоуровневый `staticcall` с газ-капом, проверкой `returndatasize() >= 0x20` и
безопасным умолчанием. Приём взят у `Treasury._readDiamondWord`. Провал чтения,
короткий ответ и пустой код на адресе — всё это читается как «вердикта нет».

Низкоуровневая форма выбрана не из вкуса: `try/catch` не лечит вид C вовсе.
Для вызова без возвращаемых данных solc ставит проверку `extcodesize` **в кадр
вызывающего, до опкода `CALL`**, и реверт летит мимо `catch` (§3 выше).
У `staticcall` такой проверки нет.

**Добавка, которой в казне нет — пол по `gasleft()`.** В казне провал чтения
фиксирует меньшую долю фаундейшна: обрушить проверку невыгодно тому, кто
обрушивает. Здесь стимул обратный — провал **открывает дверь**. Поэтому до
вызова требуется, чтобы `gasleft()` хватало на честную передачу всего капа
(кап × 64/63 плюс запас на сам вызов); не хватает — ревертит вся транзакция,
а не «ну, значит не отвечает».

### 2. `_diamondHasCode()` — на всех терпимых обращениях

Одна мысль, применённая единообразно: **пустой код на адресе диамонда обязан
читаться как «вызов не состоялся», а не как реверт всей транзакции.** Гейт
стоит на шести местах: `_updateRegistry`, `autoAwardXP`, `notifyExecutorFault`
(оба), `notifyArbiterTimeout`, `_clearDisputeClaim`, `creditDisputeFee`.

Это не расширение задачи, а её обязательная часть. При виде C
`triggerArbiterTimeout` умирал **не на 875**, а дальше — на `_updateRegistry`
внутри `_complete()`. Одной правкой строки 875 требование «деньги выходят во
всех трёх мирах» выполнить нельзя было в принципе.

Побочно чинятся и остальные пути: `release`, `triggerAutoApprove` и оба
таймаута теперь выпускают деньги и при виде C. Обещание сохранности стало
верным во всех трёх мирах, а не в двух.

Замечание про порядок: `_clearDisputeClaim` и `notifyArbiterTimeout` стоят
**после** перевода, и это их не спасало — реверт откатывает транзакцию целиком
вместе с переводом.

**Хранилища не трогали.** Новых полей нет; константы и функции в раскладке не
живут. `script/check-agreement-layout.sh` — зелёный, 30 полей, без изменений.

## Газ: замеренная стоимость и выбранный кап

| что | значение |
|---|---|
| `hasSubmittedVerdict` через диамонд, **всё холодное** (аккаунт диамонда, слот адреса фасета, аккаунт фасета, слот вердикта) | **11 064** |
| газ-кап `VERDICT_VIEW_GAS` | **100 000** |
| пол `VERDICT_VIEW_GAS_FLOOR` = кап + кап/63 + 8 000 | **109 587** |

Запас — примерно **9×**. Число 100 000 не изобретено: столько же держит
`Treasury.DIAMOND_VIEW_GAS` для чтения того же класса.

Замер живёт тестом `testVerdictViewCostSitsFarUnderTheCap` и печатается в
вывод. Кап в тесте — рукописный литерал, а не чтение из проверяемого
контракта; слишком **низкий** кап ловится не им, а поведением:
`testLiveVerdictStillBlocksTheHatch` краснеет сразу, как только чтение
перестаёт помещаться в бюджет.

### Честно про пол: сегодня он не может сработать во вред, и вот арифметика

Голодание газом при таком соотношении **недостижимо**, и это надо назвать, а не
спрятать за словом «защита». По EIP-150 вызываемому уходит `G − G/64`. Чтобы
внутренний вызов не уложился, нужно `63/64·G < 11 064`, то есть `G < 11 240`.
Но после проверки функции нужно ещё около 150 000 газа на расчёт и переводы —
столько при таком `G` взять неоткуда, транзакция умрёт следом. Атака требует,
чтобы чтение стоило дороже **шестидесяти трёх** остатков функции.

Пол всё равно поставлен: он превращает случайную арифметику в явный инвариант.
Соотношение поедет само — если чтение подорожает или хвост функции подешевеет.

Тест голодания это учитывает и утверждает **две разные вещи**: (1) ни при каком
бюджете деньги не выходят мимо живого вердикта, (2) ниже пола контракт
**отказывается читать** (`NotEnoughGasForVerdictCheck`), а не гадает.

## Мутации: кто именно покраснел

Все прогоны — полный набор (1002 теста), база зелёная.

### M1. Снять починку: вернуть голый вызов на 875

**14 красных, все 14 назначенные, все в `DiamondDeathEscrow.t.sol`:**

```
testArbiterTimeoutUnclaimed_SelectorsRemoved_MoneyOut
testArbiterTimeoutUnclaimed_FacetReverts_MoneyOut
testArbiterTimeoutUnclaimed_NoCode_MoneyOut
testArbiterTimeoutClaimed_SelectorsRemoved_MoneyOut
testArbiterTimeoutClaimed_FacetReverts_MoneyOut
testArbiterTimeoutClaimed_NoCode_MoneyOut
testHatchIsTheOnlyDoorAndItOpens_SelectorsRemoved
testHatchIsTheOnlyDoorAndItOpens_FacetReverts
testHatchIsTheOnlyDoorAndItOpens_NoCode
testDisputeRaisedMidOutageStillHasAWayOut_SelectorsRemoved
testDisputeRaisedMidOutageStillHasAWayOut_FacetReverts
testDisputeRaisedMidOutageStillHasAWayOut_NoCode
testGasBurningVerdictFacetCannotBlockTheHatch
testGasStarvationCannotForceTheHatchPastALiveVerdict
```

`testLiveVerdictStillBlocksTheHatch` при этом **остался зелёным** — и это
правильный ответ, а не пропуск: голый вызов на живом даймонде отвечает верно,
именно это и должно быть неизменным.

### M2. Снять только пол по `gasleft()`, кап оставить

**1 красный, и это назначенный:**

```
testGasStarvationCannotForceTheHatchPastALiveVerdict
  "below the floor the contract must refuse to read, not guess"
```

Ровно один — как и требовалось. Краснеет он по своей причине: без пола
маленькие бюджеты отвечают `VerdictInFlight` вместо
`NotEnoughGasForVerdictCheck`, и утверждение (2) не выполняется. Утверждение
(1) — «деньги не вышли» — остаётся верным (см. арифметику выше), поэтому тест
и разделён на две части.

### M3. Порча семантики: успешное чтение «вердикт подан» игнорируется

**3 красных, среди них назначенный:**

```
testLiveVerdictStillBlocksTheHatch            ← назначенный
  "a live verdict must still block the timeout"
testGasStarvationCannotForceTheHatchPastALiveVerdict
  "no gas budget may open the hatch past a live verdict"
test/Diamond.t.sol :: testTriggerArbiterTimeout_RevertsIfVerdictAlreadySubmitted
```

Третий — независимый замок, существовавший до этой работы, в другом наборе.
То есть поведение при живом даймонде сторожат два разных источника.

### M4. `_diamondHasCode()` всегда возвращает `true`

**9 красных, все девять — `*_NoCode_*`:**

```
testRelease_NoCode_MoneyOut
testAutoApprove_NoCode_MoneyOut
testActivationTimeout_NoCode_MoneyOut
testDeadlineTimeout_NoCode_MoneyOut
testArbiterTimeoutUnclaimed_NoCode_MoneyOut
testArbiterTimeoutClaimed_NoCode_MoneyOut
testHatchIsTheOnlyDoorAndItOpens_NoCode
testDisputeRaisedMidOutageStillHasAWayOut_NoCode
testCodelessDiamondPaysOutAndStillAnnouncesTheStaleRegistry
```

Последний — не про деньги, а про **честность ветки**: он требует, чтобы при
пустом коде сработало событие `RegistrySyncFailed`. Без него «деньги вышли»
было бы совместимо и с тем, что вызов молча объявили успешным.

### M5. Снять газ-кап (передавать весь газ)

**1 красный, назначенный:**

```
testGasBurningVerdictFacetCannotBlockTheHatch
  "a capped read cannot be starved into taking the whole transaction"
```

Кап действительно держит: без него жгущий газ фасет забирает 63/64 транзакции
и на выплату не остаётся.

### Откуда берётся ожидаемое

«Вердикт подан» ставится **действием** — вызовом `submitVerdict` через диамонд,
— а не чтением того же слота, который читает проверка. Ожидаемые исходы —
рукописные селекторы ошибок (`VerdictInFlight`,
`NotEnoughGasForVerdictCheck`). Суммы и окна остались рукописными литералами,
как и в первой редакции набора.

## Что переименовано в замке

Строки, утверждавшие отсутствие выхода, поменяли знак и имена — иначе имя
теста врало бы про его содержание:

| было | стало |
|---|---|
| `testArbiterTimeout{Unclaimed,Claimed}_*_MoneyStUCK` | `..._MoneyOut` |
| `testRelease_NoCode_MoneyStUCK` и три его брата | `..._NoCode_MoneyOut` |
| `testDisputedDealHasNoOtherExit_*` | `testHatchIsTheOnlyDoorAndItOpens_*` |
| `testDisputeCanStillBeRaisedWhileDiamondIsDead_*`, `testDisputeCannotBeRaisedWhenDiamondHasNoCode` | `testDisputeRaisedMidOutageStillHasAWayOut_*` (теперь все три вида) |

`testHatchIsTheOnlyDoorAndItOpens_*` по-прежнему стучится во все двери и
требует, чтобы они были закрыты, — половина «второй двери нет» никуда не
делась, к ней добавилась вторая половина «а единственная открывается».

## Что решено НЕ трогать, и почему

### `syncRegistry()` — голый вызов оставлен намеренно

Функция денег не двигает. Это инструмент починки рассинхрона, за которым
приходят после события `RegistrySyncFailed`. Терпимая версия отвечала бы
«готово», оставив реестр неверным, — единственный ответ, которого инструмент
починки давать не должен. Падать при мёртвом даймонде для неё правильно.
`testSyncRegistryIsBareAndDiesWithTheDiamond` остался зелёным и теперь
фиксирует решение, а не дефект. Этим объяснением снимается часть пункта 114 —
объяснением, а не правкой.

### `setArbiter()` — оценено и оставлено как есть

Мёртвая половина условия `!ok` мертва **только в недостижимом состоянии**:
позвать `setArbiter` может исключительно сам диамонд (`msg.sender != diamond` →
реверт), а значит код на диамонде в этот момент есть по построению.
Достижимый случай — снятый селектор `isRegisteredArbiter` — сегодня
отрабатывает верно: `ok == false`, реверт `ArbiterNotRegistered`.

Остаётся единственная разница: при фасете, отвечающем пустотой, падение
приходит `Panic`'ом из `abi.decode`, а не задуманной ошибкой. Обе ветки
ревертят, направление отказа безопасное (арбитра не назначили — спор просто не
взят, а выход по таймауту работает), денег путь не касается. Косметическая
правка в `Agreement` стоит новой реализации и нового деплойера — такое едет
отдельно и вместе с чем-то весомым, а не в одиночку.

### Гейт «жёсткий газ у арбитрских кнопок» — не задет, проверено

`frontend/src/lib/arbiterWritesEstimateGas.test.ts` обходит `frontend/src/`
и читает только `.ts`/`.tsx`; предмет — литерал `gas:` в вызове viem, который
отключает `eth_estimateGas` в кошельке. Solidity он не видит вовсе. Проверено
чтением его собственного `SRC_DIR`, а не доверием к формулировке задания.

## Что осталось открытым

**Пункт 113 закрыт наполовину.** Кап покрывает только чтение вердикта, и этого
хватает единственному выходу из спора. Автоапрув и `release` дорожают
по-прежнему в ~71 раз — их топят два обращения в `_complete()`
(`_updateRegistry` и `autoAwardXP`), а `testGasBurningFacetBlocksAutoApproveAtARealisticGasBudget`
остался зелёным, то есть по-прежнему фиксирует дефект.
Лечится либо капом и на них (размен: реестр молча остаётся старым), либо
переносом обращений за перевод — и то и другое трогает все пути разом, поэтому
это отдельное решение владельца.

**Пункт 114 остаётся в части формы.** Ловушка `extcodesize` никуда не делась;
её надо помнить при каждом новом обращении к диамонду из `Agreement`. Проверки
кода диамонда на входе (`Agreement.initialize`, `initFactory`) как не было, так
и нет — просто последствия её отсутствия перестали быть фатальными.

## ⚠️ Выкатка: это отдельная работа и отдельное решение

Правка `Agreement.sol` меняет **реализацию**, с которой клонируются сделки.
Значит нужны новая развёрнутая реализация и новый `AgreementDeployer`, а в
`FactoryStorage` — переключение через `setAgreementDeployer`.

**Уже созданные клоны EIP-1167 прибиты к своей реализации намертво и починку не
получат никогда.** Все спорные сделки, созданные до переключения, остаются с
голой проверкой на 875. Это не побочная деталь выкатки, а её содержание: у
живых сделок починки не будет, и решать, что с ними делать, придётся отдельно.

В этой работе выкатка не делалась и в цепь не писалось ничего.

---

# Починка, часть вторая: газ (пункт 113)

**Дата:** 2026-08-23. Правка — `src/Agreement.sol`. Замок — `test/DiamondDeathGasCaps.t.sol`
(новый файл, 25 тестов) плюс переписанные строки в `test/DiamondDeathEscrow.t.sol`
(43 теста, было 43 — два поменяли знак и имя). Полный набор: 1002 → 1027, зелёный.
В цепь ничего не писалось.

Первая половина (22 августа) закрыла чтение вердикта. Здесь закрыто остальное:
**ни одно обращение к диамонду больше не может потратить неограниченный газ.**

## Одной строкой

| | было | стало |
|---|---|---|
| закрытие сделки, здоровый даймонд | 411 688 | 411 659 — расход не изменился |
| то же при даймонде, который вместо ответа жжёт газ | ~29 791 258 — **71×** | **699 247 — 1,70×** |

Первая колонка второй строки — цифра 22 августа (`triggerAutoApprove` при бюджете
30 000 000). Вторая колонка обеих строк замерена сегодня на `release`, который своему
близнецу `triggerAutoApprove` стоит копейка в копейку.

Замер живёт тестом `testAGasEaterCanNoLongerInflateTheCostOfClosingADeal`: он меряет
обе цифры в одном прогоне, на одном стенде, и требует, чтобы вторая была меньше
удвоенной первой. Отдельно замерена цена ОДНОГО пропущенного капа — мутация M1 ниже
показывает миллиард.

## Полный список обращений к диамонду, и почему он полон

Список получен **не чтением и не grep'ом, а разбором AST** — тем же скриптом, который
теперь сторожит правило (`script/check_diamond_call_gas.py`). Скрипт обходит каждый
внешний вызов файла, поэтому «полон» здесь означает «по построению», а не «я
посмотрел внимательно». Сегодня он видит **8 вызовов под капом и 6 объяснённых**.

| где стоит | что зовёт | до денег / после | кап | пол по `gasleft()` |
|---|---|---|---|---|
| `release` → `_complete` | `updateStatus` | **до** | `REGISTRY_UPDATE_GAS` | нет |
| `release` → `_complete` | `autoAwardXP` | **до** | `XP_AWARD_GAS` | **да** |
| `triggerAutoApprove` → `_complete` | те же два | **до** | те же | XP — да |
| `resolveDispute` | `creditDisputeFee` | **до** | `DISPUTE_FEE_GAS` | **да** |
| `resolveDispute` → `_complete` | `updateStatus` + `autoAwardXP` | **до** | те же | XP — да |
| `resolveDispute` | `clearDisputeClaim` | после | `CLAIM_CLEAR_GAS` | нет |
| `triggerActivationTimeout` → `_complete` | `updateStatus` | **до** | `REGISTRY_UPDATE_GAS` | нет |
| `triggerActivationTimeout` | `notifyExecutorFault` | после | `FAULT_NOTIFY_GAS` | нет |
| `triggerDeadlineTimeout` → `_complete` | `updateStatus` | **до** | `REGISTRY_UPDATE_GAS` | нет |
| `triggerDeadlineTimeout` | `notifyExecutorFault` | после | `FAULT_NOTIFY_GAS` | нет |
| `triggerArbiterTimeout` | `hasSubmittedVerdict` | **до** | `VERDICT_VIEW_GAS` (22 авг) | да (22 авг) |
| `triggerArbiterTimeout` → `_complete`, обе ветки | `updateStatus` | **до** | `REGISTRY_UPDATE_GAS` | нет |
| `triggerArbiterTimeout` | `notifyArbiterTimeout` | после | `ARBITER_TIMEOUT_GAS` | нет |
| `triggerArbiterTimeout`, обе ветки | `clearDisputeClaim` | после | `CLAIM_CLEAR_GAS` | нет |
| `raiseDispute` → `_updateRegistry` | `updateStatus` | денег не двигает вовсе | `REGISTRY_UPDATE_GAS` | нет |
| `syncRegistry` | `updateStatus` | денег не двигает вовсе | **нет, намеренно** | нет |
| `setArbiter` | `isRegisteredArbiter` | денег не касается | **нет, намеренно** | нет |

Одна оговорка про «до денег»: на путях, где есть ожидающие extras, `_settlePending()`
возвращает их клиенту ещё раньше — то есть буквально первым центом двигается он.
«До денег» в таблице означает «до ОСНОВНОЙ выплаты», той, ради которой сделка
существует. Для газовой арифметики это ничего не меняет: сожжённый газ не
возвращается независимо от того, что уже успело перевестись.

Два последних объяснены записями в `script/diamond-call-gas.allow`; там же — четыре
вызова самого USDC (`SafeUSDC.*`, `IERC20.balanceOf`), которые под правило не подпадают:
токен это не служба сбоку, это и есть деньги.

### Находка: «после денег» безопасным местом НЕ является

Бриф исходил из того, что вызовы за переводом капа не требуют — «они уже в правильном
порядке». Замер это опроверг.

Один жгущий вызов после денег действительно переживается: `triggerActivationTimeout`
с горящим `notifyExecutorFault` при 210 000 газа проходит, потому что после перевода
остаётся только событие. Но у `triggerArbiterTimeout` таких вызовов **два подряд** —
`notifyArbiterTimeout`, затем `clearDisputeClaim`. Каждый съедает ~31/32 остатка, два
оставляют ~1/1024, транзакция умирает **и откатывает перевод вместе с собой**.

Замерено до починки: та же сцена при 500 000 газа (собственный потолок этой кнопки во
фронте) — **отказ, весь котёл остался в клоне**. Поэтому капы поставлены и на них.
Замок — `testBurningFacetsAfterTheMoneyCannotBlockArbiterTimeout`.

## Каждый кап — замеренный

Стоимость мерена против настоящих фасетов через настоящий прокси, с `vm.cool()` на
аккаунт диамонда, слот адреса фасета и сам фасет — то есть в том состоянии, в котором
вызов делается в цепи, потому что каждая транзакция начинается с холодного хранилища.
Пересчитывается на каждом прогоне (`test/DiamondDeathGasCaps.t.sol`, секция 14).

| вызов | замер (всё холодное) | кап | запас |
|---|---|---|---|
| `updateStatus` | **58 521** | 150 000 | 2,6× |
| `autoAwardXP` | **332 268** | 500 000 | 1,5× |
| `creditDisputeFee` | **56 696** | 150 000 | 2,6× |
| `notifyExecutorFault` | **14 298** | 100 000 | 7,0× |
| `notifyArbiterTimeout` | **31 814** | 150 000 | 4,7× |
| `clearDisputeClaim` | **74 746** | 200 000 | 2,7× |
| `hasSubmittedVerdict` (22 авг) | 11 064 | 100 000 | 9,0× |

Числа капов в тесте — рукописные литералы, скопированные из контракта руками. Читать
их из проверяемого контракта значило бы смотреться в зеркало (четвёртый способ).
Слишком **низкий** кап этой секцией не ловится вовсе — его ловит поведение, секция 15.

### Почему у `autoAwardXP` запас всего 1,5×, и это не небрежность

`autoAwardXP` — самый дорогой вызов из шести (332 268 против 58 521 у соседа), и его
кап задаёт **минимальный лимит газа для `release`**, потому что пол требует уметь
передать кап целиком. 500 000 — наибольший кап, при котором честный `release`
по-прежнему помещается в потолок, который фронт уже возит сегодня (660 000).

Запас в 167 732 газа — это примерно семь лишних холодных `SSTORE` у функции, в дереве
вызовов которой **нет ни одного цикла** (проверено чтением `ReputationFacet`:
`_evalPairCap`, `_evalStreakOnce`, `_awardXP`, `_penalizeXP`, `_addXP` — все O(1)).
Худший случай замерен по построению: первая в жизни сделка новой пары, когда XP,
серия, пара и счётчик уникальных пользователей пишутся в холодные слоты для обеих
сторон разом.

## Пол по `gasleft()`: где стоит и где намеренно НЕ стоит

Кап на терпимом вызове приглашает обратную атаку: подобрать лимит так, чтобы вызов не
уложился, а хвост функции уложился. Неудача поймана, сделка закрыта, разницу
забирает тот, кто подбирал лимит.

Пол стоит на двух вызовах — тех, чей провал **чего-то стоит вызывающему**:

* **`autoAwardXP`** — у контрагента пропадает XP. XP гейтит вход в реестр арбитров
  (`MIN_XP_TO_REGISTER`), то есть это способ тихо придержать чужой рост;
* **`creditDisputeFee`** — 3% арбитра остаются в котле, который вызывающий сейчас
  выиграет.

Пол намеренно **не стоит** на `_updateRegistry`, `notifyExecutorFault`,
`notifyArbiterTimeout`, `clearDisputeClaim`. Их провал: (а) объявляет себя событием,
(б) чинится кем угодно (`syncRegistry()`), (в) голодающему не приносит ничего.
И цена была бы не теоретической: пол на `_updateRegistry` поднял бы минимальный лимит
`raiseDispute` выше потолка 160 000, который фронт возит для неё сегодня, — живая
регрессия в обмен ни на что.

### Честно: сегодня голодание недостижимо арифметически

Это надо назвать, а не спрятать за словом «защита» — ровно как это сделано для пола
чтения вердикта 22 августа.

Чтобы `autoAwardXP` не уложился, нужно, чтобы на момент вызова оставалось меньше
`332 268 · 64/63 ≈ 338 000`. Но после провала у функции остаётся ~1/32 от того, что
было до вызова, — при 338 000 это ~10 500, а на перевод нужно ~30 000. Транзакция
умрёт следом, и деньги никуда не уйдут. У `creditDisputeFee` разрыв ещё шире.

Пол всё равно поставлен: он превращает случайную арифметику в явный инвариант, и
соотношение поедет само — если `autoAwardXP` подешевеет или хвост подорожает.

### Цена пола — лимит, а не расход

| | расход | минимальный лимит |
|---|---|---|
| `release`, здоровый даймонд | 411 659 | **600 980** |

Цепь берёт по факту, поэтому пользователь платит те же 411 659. Растёт цифра, которую
надо иметь на балансе и которую покажет кошелёк. `eth_estimateGas` подбирает лимит
двоичным поиском по факту успеха, поэтому живая оценка сама вернёт большее число;
страдает только запасной путь «узел оценить не смог» — см. раздел про шов.

## Регресс важнее починки: честный путь при бюджетах, которые продукт реально шлёт

Бюджеты взяты **не круглыми числами**, а потолками из `frontend/src/lib/relay.ts`
(`GAS_DEFAULTS`) — теми, что таблица возила ДО этой правки. Если честный путь
помещается в старый потолок, значит эта правка не требует миграции.

Все **семь** мест, откуда достижим `_complete`, прогнаны по отдельности, у каждого
читается строка реестра и XP (`testSite1_…` … `testSite7_…`), плюс восьмое обращение
вне `_complete` — `raiseDispute`.

| путь | минимальный лимит (замер) | потолок фронта | запас |
|---|---|---|---|
| `release` | 600 980 | 660 000 | 9,8% |
| `release` с ожидающим extra | 599 366 | 660 000 | 10,1% |
| `triggerAutoApprove` | 601 125 | 660 000 | 9,8% |
| `finalizeVerdict` → `resolveDispute` | 703 138 | 780 000 | 10,9% |
| `triggerArbiterTimeout` (заклеймлен) | 128 797 | 500 000 | 288% |
| `triggerActivationTimeout` | 90 857 | 210 000 | 131% |
| `raiseDispute` | 99 928 | 160 000 | 60% |

Ни один потолок не пробит. Первые четыре запаса тоньше, чем требует собственная
доктрина того файла (20%), — потолки подняты, см. шов.

При даймонде, который **жжёт газ на всех шести селекторах**, минимумы другие:

| путь | минимальный лимит | старый потолок фронта |
|---|---|---|
| `release` | 712 018 | 660 000 — **не помещается** |
| `finalizeVerdict` | 1 134 325 | 780 000 — **не помещается** |
| `triggerArbiterTimeout` | 467 710 | 500 000 — помещается |
| `triggerActivationTimeout` | 285 277 | 210 000 — **не помещается** |
| `raiseDispute` | 138 322 | 160 000 — помещается |

Это запасной путь, а не основной: живая оценка узла симулирует вызов и вернула бы
большее число сама. До капов те же три требовали ~30 000 000, и никакая оценка их бы
не спасла.

## Ещё одна правка: провал начисления XP больше не молчит

`_updateRegistry` при провале всегда стрелял `RegistrySyncFailed`. У `autoAwardXP`
не было ничего: XP просто не появлялся, и в цепи об этом не было ни следа. С капом
это стало опаснее — слишком тесный кап читался бы как «XP не положен».

Добавлено событие `XpAwardFailed(address agreement)`. Ручной путь назад существовал и
раньше — `ReputationFacet.claimXP(agreement)`, оставленный как legacy.

Тест `testXpIsSilentlySkippedWhenDiamondIsDead` переименован в
`testXpIsSkippedButAnnouncedWhenDiamondIsDead` — имя врало бы про содержание.

## Мутации: кто именно покраснел

Все прогоны — полный набор (1027 тестов), база зелёная. Число красных без имён замером
не считается, поэтому ниже названы имена и отдельно сказано, был ли среди них тот
замок, ради которого мутация ставилась.

### M1. Снять кап с `_updateRegistry`

**4 красных, назначенный среди них:**

```
testBurningRegistryFacetCannotBlockRelease           ← назначенный
  "a capped registry write cannot take the payout down with it"
testBothCompleteCallsBurningCannotBlockRelease
  "two capped calls in a row still leave enough to pay"
testAGasEaterCanNoLongerInflateTheCostOfClosingADeal
  "a gas eater must not be able to double the cost of a deal: 1 005 862 915 >= 823 316"
testGasBurningFacetNoLongerBlocksAutoApproveAtARealisticGasBudget
```

Третья строка — сама по себе результат: без одного этого капа закрытие сделки при
жгущем даймонде требует **миллиард газа**, то есть больше двадцати блоков Base.

### M2. Снять кап с `autoAwardXP`

**4 красных, назначенный среди них:**

```
testBurningXpFacetCannotBlockRelease                 ← назначенный
  "a capped XP award cannot take the payout down with it"
testBothCompleteCallsBurningCannotBlockRelease
testAGasEaterCanNoLongerInflateTheCostOfClosingADeal  (1 005 373 264 >= 823 316)
testGasBurningFacetNoLongerBlocksAutoApproveAtARealisticGasBudget
```

### M3. Снять пол `gasleft()` у `autoAwardXP`, кап оставить

**1 красный, и это назначенный:**

```
testTheXpFloorRefusesByNameRatherThanByLuck
  "below the floor the contract must refuse outright, not skip the XP"
```

**`testGasStarvationCannotRobTheExecutorOfXp` при этом остался ЗЕЛЁНЫМ, и это надо
назвать вслух.** Он перебирает бюджеты от 120 000 до 660 000 и требует: если `release`
прошёл — XP на месте. Без пола это утверждение всё равно верно, по арифметике из
раздела выше: бюджет, при котором `autoAwardXP` не укладывается, не оставляет и на
перевод, так что «прошёл» и «XP пропал» несовместимы сегодня и без пола.

То есть тест не мёртвый (утверждение он проверяет настоящее), но охраняет он
арифметику, а не пол. Пол охраняет второй тест — тем, что требует отказа **по имени
своего вызова**, а не «как-нибудь». Объяснение записано прямо в шапке секции 17, чтобы
следующий читатель не принял зелёный за доказательство.

### M4. Поставить кап заведомо малым: `REGISTRY_UPDATE_GAS` 150 000 → 20 000

**32 красных. Назначенные — все восемь про реестр:**

```
testSite1_ReleaseAtShippedBudgetKeepsRegistryAndXpTrue      "registry must say COMPLETED, not stay ACTIVE: 0 != 1"
testSite2_AutoApproveAtShippedBudgetKeepsRegistryAndXpTrue  "registry must say COMPLETED: 0 != 1"
testSite3_ResolveDisputeAtShippedBudgetKeepsRegistryAndXpTrue "registry must say RESOLVED: 0 != 4"
testSite4_ActivationTimeoutAtShippedBudgetKeepsRegistryTrue  "registry must say REFUNDED: 0 != 2"
testSite5_DeadlineTimeoutAtShippedBudgetKeepsRegistryTrue    "registry must say REFUNDED: 0 != 2"
testSite6_ArbiterTimeoutUnclaimedAtShippedBudgetKeepsRegistryTrue "registry must say REFUNDED: 0 != 2"
testSite7_ArbiterTimeoutClaimedAtShippedBudgetKeepsRegistryTrue   "registry must say REFUNDED: 0 != 2"
testRaiseDisputeAtShippedBudgetKeepsRegistryTrue            "registry must say DISPUTED: 0 != 3"
```

плюс `testBaselineDiamondCallsAreLive` и `testBurningXpFacetCannotBlockRelease`
(«the registry still moved»).

**Тесты про злой фасет остались ЗЕЛЁНЫМИ** — ровно то, чего требовал бриф: краснеет
регресс, а не защита, значит кап стоит там, где надо.

Оставшиеся 22 красных — независимый оракул, которого я не заказывал: чужие сюиты
падают с `ActiveDealExists()`, потому что реестр не убрал пару из активных и вторая
сделка той же пары больше не создаётся. Реестр действительно устарел, а не «тест так
написан».

### M5. Снять капы с двух вызовов, стоящих ПОСЛЕ денег

**1 красный, назначенный:**

```
testBurningFacetsAfterTheMoneyCannotBlockArbiterTimeout
  "two capped calls behind the payout must not roll it back"
```

### M6. Снять пол у `creditDisputeFee` — и первая редакция дала НОЛЬ

Первый прогон этой мутации дал **0 красных**. Пол существовал, был написан, читался
как защита — и не был подключён ни к чему. Ровно тот класс, ради которого правило
«сними правку, назови число красных» и заведено.

Причина оказалась в шве между двумя полами: оба ревертили **одной и той же** ошибкой
`NotEnoughGasForDiamondCall()`, оба стоят на одном пути, и тест, смотревший на
селектор ошибки, не мог отличить отказ одного от отказа другого. Убери любой — второй
ответит за него, и тест останется зелёным.

Починено правкой самой ошибки: она стала нести **селектор отказанного вызова**,
`NotEnoughGasForDiamondCall(bytes4 diamondCall)`. Это и диагностике полезно (в цепи
видно, какой именно вызов не поместился), и делает оба пола различимыми.

Повтор после починки — **1 красный, назначенный:**

```
testTheDisputeFeeFloorRefusesByNameRatherThanByLuck
  "the fee floor must refuse in its own name, not leave the XP floor to answer for it"
```

Повтор M3 после той же правки — по-прежнему ровно 1 красный, `testTheXpFloorRefuses…`.

## Гейт: правило переселено из голов в проверку

`script/check-diamond-call-gas.sh` + `script/check_diamond_call_gas.py` +
`script/diamond-call-gas.allow`. Пятый статический гейт репозитория.

Правило: в `src/Agreement.sol` внешний вызов идёт под `{gas: ...}` либо объяснён
записью в allow-файле — с причиной, начинающейся дословно с «без капа, потому что:»,
и с пиннутым счётчиком. Формат неудобен намеренно, как у соседнего гейта ERC-2771:
внести исключение нельзя, не сказав вслух почему.

На чистом дереве: **8 вызовов под капом, 6 объяснены**.

### Сначала — ответ на «а если через переменную?»

Два гейта репозитория уже слепли ровно так: сканер узнавал написание, а не то, на
что оно ссылается. Поэтому здесь разбирается AST solc, признак берётся **типовой**,
и это проверено порчей — три формы, гейт прогнан на каждой:

| порча | что ответил гейт |
|---|---|
| `{gas: ...}` просто снят с `_updateRegistry` | **красный**: «БЕЗ КАПА и без записи: `Agreement._updateRegistry :: contract ISignatureRegistry.updateStatus`» |
| тот же вызов **через переменную**: `address target = diamond; ISignatureRegistry reg = ISignatureRegistry(target); reg.updateStatus(...)`, кап снят | **красный**, то же сообщение |
| в ассемблере `staticcall(gas(), ...)` вместо `staticcall(gasCap, ...)` | **красный**: «БЕЗ КАПА и без записи: `Agreement._verdictInFlight :: assembly.staticcall`» |

Слова «diamond» в разборе нет вообще — опознаётся тип получателя, а не имя
переменной. Поэтому вторая строка и работает.

**Случай, который сканер разобрать не смог, обязан краснеть, а не молчать.** Внешний
вызов, не отнесённый ни к одной форме, валит гейт отдельным сообщением
«НЕ КЛАССИФИЦИРОВАН». Это записано в коде, а не только в намерении.

### Границы гейта, названные вслух

* **Один файл.** `Agreement` — единственный контракт репозитория, который
  одновременно держит чужие деньги на своём балансе и зовёт диамонд снаружи. Фасеты
  живут ВНУТРИ диамонда, у них этого шва нет. Про казну — пункт 115 долгов.
* **Внутри своего файла область закрыта:** гейт смотрит на ВСЕ внешние вызовы, а не
  на список известных интерфейсов, поэтому новый интерфейс, дописанный завтра, обязан
  либо нести кап, либо попасть в allow-файл.
* **Гейт не проверяет, что кап ЗАМЕРЕН.** Он видит наличие `{gas: ...}`, но не то,
  что число за ним имеет отношение к действительности; слишком тесный кап ловит
  поведение (секция 15 тестов), а не гейт.

## Шов с фронтом: пол в цепи меняет минимальный лимит, а лимит живёт в чужом файле

Это тот самый промежуток, у которого нет хозяина. В цепи появился порог; во фронте
лежит таблица потолков, которой этот порог никто не сообщал.

`frontend/src/lib/relay.ts`, `GAS_DEFAULTS` — потолки, уходящие в цепь, **когда
`eth_estimateGas` не смог оценить**. Живая оценка сама вернула бы новое число; эта
таблица — нет, она рукописная.

Проверены все записи, которые касаются путей с полом. Честный путь помещается в
каждую из них и сегодня — миграции эта правка не требует. Но четыре записи подняты
до запаса, которого требует собственная доктрина того файла (замер × 1.19 × 1.2):

| запись | было | стало | почему |
|---|---|---|---|
| `release` | 660 000 | **900 000** | запас был 9,8%; заодно накрывает жгущий случай (712 018) |
| `triggerAutoApprove` | 660 000 | **900 000** | то же |
| `finalizeVerdict` | 780 000 | **1 200 000** | запас был 10,9%; накрывает жгущий случай (1 134 325) |
| `resolveDispute` | 200 000 | **900 000** | **был сломан задолго до этой правки**, см. ниже |

### Побочная находка: `resolveDispute: 200 000` врал и до пола

`resolveDispute` идёт через тот же `_complete`, что и `release`, то есть на первой
сделке пары начисляет XP обеим сторонам в холодные слоты. Замер здорового пути —
**462 016** против отсечки 200 000. Значит запасной путь ронял арбитрские кнопки
«вернуть клиенту» / «заплатить исполнителю» в out of gas **всегда**, когда узел не
смог оценить. Это не регрессия от этой работы; найдено при замере капов и починено
здесь же, потому что оставлять известно сломанное рядом с чинимым — хуже.

### Кто теперь этот шов проверяет

`frontend/src/lib/claimGasCeiling.test.ts` — новый блок. Он читает `GAS_DEFAULTS` из
текста `relay.ts` и сверяет с **рукописной** таблицей замеренных минимумов из forge.
Числа в него вписаны руками из отдельного прогона, а не импортированы ни из таблицы,
ни из контракта: ожидаемое и фактическое приходят из разных мест.

Честно про его границу: шов проверяется **в одну сторону каждым замком**. Опустят
потолок во фронте — покраснеет этот тест. Поднимут кап в контракте так, что минимум
перерастёт потолок, — покраснеет forge-тест секции 15, потому что его бюджеты это те
же потолки, вписанные литералами. Чего не видит никто: согласованную, но неверную
правку обоих чисел разом. Заведено пунктом 116 долгов.

`frontend/src/lib/arbiterGaslessWrites.test.ts` пиннил `finalizeVerdict` литералом —
литерал обновлён вместе с таблицей, с объяснением в комментарии.

Полный прогон фронта после правок: **3134 теста, зелено** (было 3130 — четыре новых).

## Что решено НЕ делать, и почему

### Не переносить обращения за перевод

Второй вариант лечения, названный в постановке. Отвергнут замером: перенос **не
лечит**. Вызовы `notifyArbiterTimeout` и `clearDisputeClaim` уже стоят после перевода —
и без капов два жгущих подряд всё равно роняли транзакцию вместе с переводом (500 000
газа, весь котёл остался в клоне). Порядок помогает от реверта, но не от расхода;
от расхода помогает только потолок. Раз потолок нужен в любом случае, менять порядок
на всех путях разом — риск без выигрыша.

### Не ставить пол на реестр

Разобрано выше: провал объявлен событием, чинится кем угодно, голодающему не приносит
ничего, а цена — сломанный `raiseDispute` при потолке 160 000.

### Не расширять гейт на `src/Treasury.sol` в этой работе

Казна тоже зовёт диамонд снаружи. Её денежный путь уже под капом
(`_readDiamondWord` / `DIAMOND_VIEW_GAS`, 22 августа), но три вьюхи —
`getDAOAddress`, `getUniqueActiveUsers`, `getVaultBalance` — зовутся без капа.
Разбирать, какие из них стоят перед деньгами, — отдельная работа; область гейта
названа вслух в его шапке и заведена пунктом 115.

## Раскладка хранилища не тронута

Новых полей нет: только константы, приватные функции, одно событие и один аргумент у
ошибки. `script/check-agreement-layout.sh` — зелёный, 30 полей, без изменений.

**Все одиннадцать гейтов прогнаны последовательно** (параллельно нельзя: два из них
дерутся за `forge build`). Двое из одиннадцати поймали настоящие пропуски этой работы,
и это стоит записать — они не декоративные:

* `check-subgraph-arbiter-events` — `Agreement.sol` вырос с 21 события до 22
  (`XpAwardFailed`), а пин в `script/subgraph-arbiter-events.allow` остался прежним.
  Пришлось сказать вслух, почему новое событие НЕ относится к ленте ответственности
  арбитров: оно называет сделку, а не арбитра, и это ровно тот же капкан, что описан
  в том же файле про `ArbiterTimedOut`;
* `check-decisions-log` — пункт долгов помечен закрытым, а `docs/DECISIONS.md` не
  тронут. Решение записано разделом 35: размен «реестр может молча устареть, лишь бы
  деньги вышли» — тот самый, который решение 34 откладывало как отдельное.

## Файлы

| файл | что с ним |
|---|---|
| `src/Agreement.sol` | шесть капов, два пола, событие `XpAwardFailed`, ошибка с аргументом |
| `test/DiamondDeathEscrowBase.sol` | **новый** — общая оснастка, вынесена из `.t.sol` |
| `test/DiamondDeathEscrow.t.sol` | секции 1-13, два теста поменяли знак и имя |
| `test/DiamondDeathGasCaps.t.sol` | **новый** — секции 14-17, 25 тестов |
| `script/check-diamond-call-gas.sh` | **новый** гейт |
| `script/check_diamond_call_gas.py` | **новый** — разбор AST |
| `script/diamond-call-gas.allow` | **новый** — шесть объяснённых исключений |
| `script/subgraph-arbiter-events.allow` | пин `Agreement.sol` 21 → 22 с объяснением |
| `frontend/src/lib/relay.ts` | четыре потолка `GAS_DEFAULTS` |
| `frontend/src/lib/claimGasCeiling.test.ts` | замок на шов |
| `frontend/src/lib/arbiterGaslessWrites.test.ts` | обновлён пиннутый литерал |
| `docs/DECISIONS.md` | раздел 35 — размен, который откладывало решение 34 |
| `docs/OPEN-ITEMS.md` | 113 закрыт, 115 и 116 заведены |

### Почему набор пришлось разрезать на два файла

Секции 14-17 удвоили `DiamondDeathEscrow.t.sol`, и solc перестал собирать контракт
такого размера: `Internal compiler error: Tag too large for reserved space`. Оснастка
вынесена в `DiamondDeathEscrowBase.sol` без изменений, тесты разъехались по двум
файлам. Это не рефакторинг ради красоты — без него набор просто не компилируется.

## Что осталось открытым

**Пункт 113 закрыт.** Ни одно обращение к диамонду из `Agreement` больше не тратит
неограниченный газ, 71× превратились в 1,70×, и правило сторожится гейтом.

**Пункт 114 остаётся в части формы**, без изменений с 22 августа: ловушка
`extcodesize` никуда не делась, проверки кода диамонда на входе (`Agreement.initialize`,
`initFactory`) как не было, так и нет.

**Новое, заведено этой работой:**

1. **Пункт 115 — казна вне области гейта:** три вьюхи `Treasury` зовут диамонд без капа.
2. **Пункт 116 — шов «потолок фронта ↔ пол в цепи»** прикрыт двумя замками, ни один не
   видит обе стороны. Пока это лучше, чем было (не видел никто), но не полно.
3. **Пол защищает от недостижимого.** Сегодня голодание газом не проходит и без него;
   если кто-то решит, что цена (минимальный лимит `release` 600 980 вместо ~420 000)
   не стоит инварианта, — это осознанное решение, а не недосмотр.

## ⚠️ Выкатка

Правка меняет **реализацию**, с которой клонируются сделки: нужны новая развёрнутая
`Agreement`, новый `AgreementDeployer` и переключение через `setAgreementDeployer`.
**Уже созданные клоны прибиты к своей реализации намертво и капов не получат никогда.**

Порядок здесь безразличен в обе стороны: поднятые потолки фронта работают и со старой
реализацией (просто выше нужного), а новая реализация помещается и в старые потолки.
Это редкий случай, когда шов не диктует очерёдность, — но проверено это замером, а не
предположено.

В этой работе выкатка не делалась и в цепь не писалось ничего.
