/**
 * chatEnvelopeOneTimeKeyTypeBans.ts — фикстура ЗАПРЕЩЁННЫХ ПОДСТАНОВОК для
 * фирменного типа `OneTimeKey`.
 *
 * ⚠️ ПОЧЕМУ ЭТО НЕ `*.test.ts`. `frontend/tsconfig.json` исключает из
 * программы tsc любые `*.test.ts`, по любому пути — замерено: файл
 * `__probe.test.ts` с `const n: number = "строка"` даёт `npm run type-check`
 * НОЛЬ ошибок и код выхода 0, тот же текст в обычном `.ts` даёт TS2322 и код
 * 2. Фикстура, положенная в тест-файл, была бы замком, который зеленеет
 * всегда.
 *
 * (⚠️ раз уж речь про эту запись: буквальный `*` сразу за `/` внутри ЭТОГО
 * же блочного комментария закрыл бы его досрочно — ровно то, что сломало
 * компиляцию черновика этого файла в предыдущем заходе. Поэтому глоб-паттерн
 * `**` + `/` здесь не написан слитно нигде в этом файле.)
 *
 * ⚠️ ПОЧЕМУ ФИРМЕННЫЙ ТИП, А НЕ ПРОВЕРКА. Разовый ключ (32 байта), вектор
 * инициализации (12), запечатанный слот (80) и ключ вложения (32) — все
 * `Uint8Array` и структурно неотличимы. Прецедент замерен в этом проекте:
 * перестановка `boxKey`/`signKey` в реальном вызове дала 0 красных из 1826
 * (`arbiterChatKey.ts:28-42`). Здесь цена ошибки та же: печать на ключ подписи
 * вместо ключа шифрования даёт контейнер, который арбитр не откроет, а
 * предъявитель видит «сдано».
 *
 * Каждый `@ts-expect-error` ниже — утверждение «это НЕ компилируется». Он
 * двусторонний: снимешь директиву — появится ошибка подстановки; снимешь клеймо
 * с типа — директивы, которые держались КЛЕЙМОМ (запреты 1-4), станут ЛИШНИМИ
 * (TS2578), то есть ровно **4** из 7, а не все семь: запрет 5 держится
 * `strictNullChecks`, запреты 6-7 — тем, что строка не `Uint8Array` (мутация 20,
 * число пересчитано под договор v2). Молча выродиться в ничто фикстура не может.
 *
 * Импортируется тестом (`chatEnvelopeOneTimeKey.test.ts`) НЕ ради поведения, а
 * чтобы удаление этого файла краснело и в `npm test`: type-check от удаления
 * запретов молчит — замерено (мутация 21).
 *
 * ⚠️ ЗАПРЕТЫ 6 И 7 — ПРО КОДИРОВКУ, и они здесь по исправлению 2 договора v2.
 * `keys[].forArbiter` и `frames[].frame` в контейнере — base64-СТРОКИ. Самый
 * дешёвый способ убить стык — передать строку туда, где ждут байты: Задача 5
 * писала base64, Задача 6 читала hex, обе были зелёные, стык был мёртв. Строка
 * не Uint8Array ни при каком клейме, поэтому эти два запрета переживают снятие
 * клейма (см. мутацию 20: TS2578 их не касается — там их ровно 4, а не 7) — и
 * это ровно то, чего от них хотят.
 */
import {
  openEnvelopeWithOneTimeKey,
  recoverOneTimeKey,
  toOneTimeKey,
  type OneTimeKey,
} from './chatEnvelope';
import type { ChatKeypair } from './chatCrypto';

/** Сколько запретов стоит ниже. Читается тестом, чтобы «фикстуру выпотрошили»
 *  было видно и без type-check. Число записано руками — производное от самой
 *  фикстуры доказывало бы только «сколько-то запретов есть» (исправление 12
 *  договора v2: ожидаемое число пишется руками, из модуля берётся измеряемое). */
export const FORBIDDEN_SUBSTITUTIONS = 7;

/** Клеймёный ключ, добытый ЕДИНСТВЕННОЙ дверью. Участвует в поведении: тест
 *  подставляет его настоящему конверту и обязан получить `bad_key` — то есть
 *  клеймо не даёт права на содержимое, оно даёт право быть переданным. */
export const MINTED_KEY: OneTimeKey | null = toOneTimeKey(new Uint8Array(32).fill(7));

const envelope = new Uint8Array(300);
const plainKey = new Uint8Array(32);
const iv = new Uint8Array(12);
const sealedBlob = new Uint8Array(80);
const keypair: ChatKeypair = { publicKey: new Uint8Array(32), privateKey: new Uint8Array(32) };

/** Ровно то, что лежит в контейнере: base64 БЕЗ `0x` (исправление 2 договора v2).
 *  Тип — `string`, и это несущее: подстановка строки туда, где ждут байты, —
 *  самый дешёвый способ убить стык между Задачами 5 и 6. */
const b64FromContainer: string = 'q6urq6urq6urq6urq6urq6urq6urq6urq6urq6ur';

/** Никогда не вызывается. Её работа — быть проверенной тип-чекером. */
export async function forbiddenSubstitutionsMustNotCompile(): Promise<void> {
  const key = toOneTimeKey(plainKey);
  if (!key) return;

  // РАЗРЕШЕНО: клеймёный ключ остаётся обычными байтами там, где ждут байты.
  await openEnvelopeWithOneTimeKey(envelope, key);

  // 1. обычные 32 байта вместо клеймёного ключа — самый вероятный промах
  //    Задачи 6: `openSealed` отдаёт голый Uint8Array, и соблазн передать его
  //    прямо сюда, минуя `toOneTimeKey`, максимален.
  // @ts-expect-error разовый ключ нельзя подменить непроверенными байтами
  await openEnvelopeWithOneTimeKey(envelope, plainKey);

  // 2. вектор инициализации вместо ключа — 12 байт, тот же тип
  // @ts-expect-error вектор инициализации — не разовый ключ
  await openEnvelopeWithOneTimeKey(envelope, iv);

  // 3. запечатанный слот (80 байт) вместо вскрытого ключа
  // @ts-expect-error запечатанный слот — не разовый ключ
  await openEnvelopeWithOneTimeKey(envelope, sealedBlob);

  // 4. переставленные аргументы (ровно тот класс, что дал 0 красных из 1826)
  // @ts-expect-error конверт на месте ключа
  await openEnvelopeWithOneTimeKey(key, envelope);

  // 5. результат добычи БЕЗ проверки на null — «не открылось» выдаётся за ключ
  // @ts-expect-error OneTimeKey | null там, где обязан быть OneTimeKey
  await openEnvelopeWithOneTimeKey(envelope, await recoverOneTimeKey(envelope, keypair));

  // 6. base64-СТРОКА из `keys[].forArbiter` вместо раскодированных байт —
  //    четырнадцатый случай класса «кодировка» (исправление 2 договора v2).
  //    Раскодировать обязан вызывающий: `bytesFromB64` → `openSealed` →
  //    `toOneTimeKey`, и только потом сюда.
  // @ts-expect-error base64-строка — не ключ; раскодировать обязан вызывающий
  await openEnvelopeWithOneTimeKey(envelope, b64FromContainer);

  // 7. base64-СТРОКА из `frames[].frame` вместо байт конверта — тот же класс с
  //    другой стороны: строка сюда доезжает молча, если её не раскодировали.
  // @ts-expect-error base64-строка — не байты конверта
  await openEnvelopeWithOneTimeKey(b64FromContainer, key);
}
